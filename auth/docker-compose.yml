version: '3'

networks:
  kafka_app-net:
    external: true

services:
  auth:
    container_name: auth
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    networks:
      - kafka_app-net
